# @pennypot/web

Reserved for the PennyPot webapp (frontend). Not yet implemented.

When scaffolding here, add a `package.json` named `@pennypot/web` so it joins the
pnpm workspace, and consume the contract ABIs/addresses from `@pennypot/contracts`.
